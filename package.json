{
  "name": "socialfeed",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd backend && node server.js",
    "build": "cd frontend && npm ci && npm run build",
    "postinstall": "cd backend && npm ci && cd ../frontend && npm ci && npm run build"
  }
}
